import React from 'react';
import { CurrencySettings } from 'settings/components';

const SettingsLayout = (props) => (
  <div>
    <h3>Settings</h3>
    <CurrencySettings />
  </div>
);

export default SettingsLayout;
